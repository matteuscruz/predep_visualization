__pycache__/
*.py[cod]
.env
.venv/

# NetCDF pesados excluídos do git (pipeline gera Parquet, formato usado no viewer)
*.nc

# resultados: apenas Parquet no git (NC excluídos pela linha acima)
# PNGs de plots gerados dentro de results/ (resíduo de cópia) não entram
results/**/plots/
